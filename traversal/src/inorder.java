/*package traversierung;

public class inorder {

    public Binearbaum b = new Binearbaum;

    public void InOrder(Binearbaum b) {
        if (b.getLeftTree() != null) {
            InOrder(b.getLeftTree());
        }

        System.out.println(b.getRootItem().toString());
        if (b.getRightTree() != null) {
            InOrder(b.getRightTree());
        }
    }
}
*/